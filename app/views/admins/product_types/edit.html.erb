<h2>ジャンル編集</h2>

<%= form_with(model[:admins, @product_type], local: true) do |f| %>
  <div class='field'>
    ジャンル名<%= f.text_field :name %>
  </div>
  <div class='field'>
    <%= f.submit '変更を保存' %>
  </div>
<% end %>