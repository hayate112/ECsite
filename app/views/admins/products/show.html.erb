<h2>商品詳細</h2>

<div>
  商品名<%= @product.name %>
</div>
<div>
  商品説明<%= @product.descripiton %>
</div>
<div>
  ジャンル<%= @product.product_type.name %>
</div>
<div>
  税込価格<%= (@product.price * 1.08) %>
  （税抜価格）<%= @product.price %>
</div>
<div>
  販売ステータス<%= @product.is_active %>
</div>
