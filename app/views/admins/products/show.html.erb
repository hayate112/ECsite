<h2>商品詳細</h2>


<div>
  <%= attachment_image_tag(@product, :image, fallback: "no_image.jpg", size: "250x250") 
</div>
<div>
  商品名<%= @product.name %>
</div>
<div>
  商品説明<%= @product.description %>
</div>
<div>
  ジャンル<%= @product.product_type.name %>
</div>
<div>
  税込価格<%= (@product.price * 1.08).round %>
  （税抜価格）<%= @product.price %>
</div>
<div>
  販売ステータス
  <% if @product.is_active == true %>
    販売中
  <% else %>
    販売停止中
  <% end %>
</div>

<%= link_to '編集する', edit_admins_product_path(@product) %>
